Para adicionar as dependências abra o terminal no VS Code
e digite:

**** ( ) para liberar o proxy

**** npm install

Crie um arquivo .env com suas credenciais de acesso ao banco de dados
DB_HOST = ????
DB_USER = ????
DB_PASSWORD = ????
DB_PASSWORDITB = ????
DB_NAME = ????
BD_PORT = ????



nome 
email 
nome user - não poder ter 2 no banco com o mesmo nome de usuario 
data de nascimento 
senha 
confirma senha não pode copia e colar no confirma a senha 



                    <!-- <article class="form-imput"> -->
                    <!--   <label id="labelCidade" for="cidade">Cidade <span>*</span> </label>
                        <input type="text" list="lista"  placeholder="cidade " value="<%= valores.cidade %>"  id="cidade" name="cidade" class="<%= cssErroBorda.user %>">
                        <span class="<%= cssErro.cidade %>"><%= msgErro.cidade %></span><br>
                        <datalist id="lista">
                        </datalist> -->
                    <!-- <script>
                            const callApi = async () => {
                                const data = await fetch("https://servicodados.ibge.gov.br/api/v1/localidades/estados/35/municipios",);
                                const dataJson = await data.json();
                                console.log(dataJson[0]);
                                let lista = document.querySelector("#lista");
                                console.log(lista);
                                 dataJson.forEach(item => {
                                    let element = document.createElement("option");
                                    element.value = item.nome;
                                    console.log(element);
                                    lista.appendChild(element);
                                   
                                });
                            }
 
                            callApi();
                        </script> -->

                    <!-- </article> -->

                    <!-- <img alt="Eye Icon" title="Eye Icon" src="../../img/Login/eye-off.svg" class="input__icon"> -->


















                    <%
    var msgErro = {nome:""}
    var cssErro = {nome:""}
    var cssErroBorda = {nome:""}
    let valorCampos = {nome:""}
    if(dados && dados!=null){
        valorCampos = dados
    }

    if(listaErros){
        listaErros.errors.forEach((itemErro)=>{
            if(itemErro.path == "nome"){
                msgErro.nome = itemErro.msg
                cssErro.nome = "erroForm"
                cssErroBorda.nome = "erroInput"
                %>
                    <script>notify('Verifique o campo nome', '<%= msgErro.nome %>', 'error'  )</script>
                <%
            }
        });
    } 
   
%>